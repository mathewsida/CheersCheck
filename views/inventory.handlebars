<main>
    <h2 class="your-post-title">Your Liquor Cabinet:</h2>

    <div id="dash-comments">
        <ul>
            {{#each comments}}
            <li class="comment-list">" {{comment_text}} " - {{user.username}}</li>
            {{/each}}
        </ul>
    </div>
    </br>
    {{/each}}

    </ul>
    {{else}}
    <p>You haven't added any inventory yet.</p>
    {{/if}}
    </br>
    <button type="submit" class="btn button" id="dash-new-post-btn">+ New Post</button>


</main>
<script src="/js/inventory.js"></script>

{{#if posts}}

<div class="inventory-cabinet">
    <div class="drawer"></div>
    <div class="drawer"></div>
    <div class="drawer"></div>
</div>


{{!-- inventory table idea --}}
{{!--
<div class="container-inv">
    <div class="inventory-table">
        <table>
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Liquor Type</th>
                    <th>Volume Size</th>
                    <th>Description</th>
                </tr>
            </thead>
            <tbody>
                {{#each inventory}}
                <tr>
                    <td>{{name}}</td>
                    <td>{{liquor_type}}</td>
                    <td>{{volume_size}}</td>
                    <td>{{description}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>
Copy code
.container {
display: flex;
justify-content: center;
align-items: center;
height: 100vh;
}

.inventory-table {
width: 80%;
margin: auto;
}

table {
width: 100%;
border-collapse: collapse;
border-spacing: 0;
}

th, td {
padding: 10px;
text-align: left;
border-bottom: 1px solid #ddd;
}

th {
background-color: #f2f2f2;
}

tr:hover {
background-color: #f5f5f5;
} --}}